[project]
name = "mcp-weather-service"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">3.11"
dependencies = [
    "gradio[mcp]>=6.11.0",
    "httpx>=0.28.1",
    "mcp[cli]>=1.26.0",
]
